@model int?
@(
    Html.Kendo().DropDownListFor(m => m)
        //use the data-skip attribute to prevent adding the default value binding and add the data-bind attribute for the custom binder
                //.HtmlAttributes(new { data_skip = "true", data_bind = "nullableValue: account_status_ref"})
                .OptionLabel("--Chọn trạng thái--")
                .BindTo((System.Collections.IEnumerable)ViewData["AccountStatus"])
                .DataValueField("Value")
                .DataTextField("Name")
)